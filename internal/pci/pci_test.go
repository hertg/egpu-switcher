package pci_test

/*func TestMain(m *testing.T) {
	gpus := pci.ReadGPUs()
	for _, gpu := range gpus {
		displays, _ := gpu.HasDisplaysConnected()
		fmt.Println(gpu.Address(), displays)
	}
	panic("err")
}
*/
